#!/usr/bin/env zsh

set -euo pipefail

if [ -f "/etc/arch-release" ]; then
    sudo pacman -Syu
    yay -Syu
    flatpak update
fi

if [[ "$OSTYPE" == "darwin"* ]]; then
    brew update
    brew upgrade
fi

rustup update
doom upgrade
